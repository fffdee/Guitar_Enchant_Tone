# ESP-GT-XFORM PC 训练/验证框架依赖
# 核心管线仅需 numpy + torch，其余为可选增强项。

# --- 必需（核心管线）---
numpy>=1.23
torch>=2.0            # 训练；CUDA 版本请按显卡自行安装

# --- 可选（增强：配置/进度条/可视化）---
pyyaml>=6.0           # 读取 configs/*.yaml（缺失时使用代码内默认配置）
tqdm>=4.64            # 训练进度条（缺失时退化为简单打印）
matplotlib>=3.5       # 频谱/曲线可视化（scripts 与上位机 tools/xform_studio.py）

# --- 可选（上位机 GUI：tools/xform_studio.py）---
PyQt5>=5.15           # 图形界面（数据导入/预处理/训练可视化/推理输出一体化）

# --- 可选（对照/扩展，当前实现未强依赖）---
# soundfile>=0.12     # 非 16bit/多格式音频读写（核心用标准库 wave 即可）
# librosa>=0.10       # 参考实现对照（核心特征已用 numpy 自研，便于移植到 C）
# scipy>=1.10         # 滤波器设计等（核心未使用）
